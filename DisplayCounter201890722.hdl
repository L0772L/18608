CHIP DisplayCounter201890722 {
    IN inc, reset;
    OUT a, b, c, d, e, f, g;

    PARTS:
    Counter(inc=inc, reset=reset, A=A, B=B, C=C, D=D);

    Decoder201890722(
        A=A, B=B, C=C, D=D,
        a=a, b=b, c=c, d=d, e=e, f=f, g=g
    );
}

