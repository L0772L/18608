CHIP Counter {
    IN inc, reset;
    OUT out[16];

    PARTS:
    Register(in=next, out=out, load=load);
    Inc16(in=out, out=plusOne);
    Mux16(a=out, b=plusOne, sel=inc, out=maybeInc);
    Mux16(a=maybeInc, b=false, sel=reset, out=next);
    Or(a=inc, b=reset, out=load);
}

